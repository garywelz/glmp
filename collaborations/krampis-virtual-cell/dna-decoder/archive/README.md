# Phase B1 archive

Synthetic lac prototype (`lac_operon_region_synthetic_prototype.fa`) is written here on Jetson
when `scripts/phase_b1_reanchor.py` runs. The file is gitignored under `sequences/`; this folder
holds the renamed provenance copy only on the decoder host.
